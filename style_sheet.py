style_cont = {
    'display': 'flex',
    'width': '50%',
    'flexDirection': 'column',
    'margin': '5px auto',
    'alignItems': 'center',  # This centers the button horizontally
    'padding': '20px',  # Add some padding around the container
}

flex_container_style = {
    'display': 'flex',
    'flexDirection': 'row',  # Align children in a row
    'alignItems': 'flex-start',  # Align items at the start of the flex container
    'gap': '20px',  # Space between the containers
    'width': '100%'
}

style_cont_graph = {
    'display': 'flex',
    'flexDirection': 'column',
    'alignItems': 'center',  # This centers the button horizontally
    'padding': '20px',  # Add some padding around the container
    'gap': '10px',  # Adds space between children
    'flex': '1'
}

style_input = {
    'margin': '3px 0',  # Adds top and bottom margin for spacing between inputs
    'width': '100%',  # Use the full container width
}

style_btn = {
    'margin': '5px auto',  # Top margin and horizontally centered
    'display': 'block',  # Ensure margin auto centers the button
    'padding': '10px 20px',  # Padding inside the button for better appearance
}

style_title = {
    'textAlign': 'center',  # Centers the title text
    'marginBottom': '20px',  # Adds some space between the title and the first input
    'marginTop': '40px',
}

style_text = {
    'textAlign': 'left',  # Centers the title text
    'marginBottom': '20px',  # Adds some space between the title and the first input
}